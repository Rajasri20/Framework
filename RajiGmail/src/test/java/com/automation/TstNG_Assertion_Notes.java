package com.automation;

public class TstNG_Assertion_Notes {
	
	// In REAL TIME, instead of "sysout" we USE Assertions Validations  
	
	/* ASSERTS Class
	 * 
	 * Assertion is nothing but 'VALIDATION'
	 * 
	 * ASSERTS are 2type
	 * 
	 * A. Assert Or HardAssert
	 * B. SoftAssert
	 * 
	 * 
	 * A. Assert Or HardAssert
	 * 
	 * 
	 * 1. static assertNull(Object, Object)
	 * 
	 * 'assertNull' method verifies whether the given INPUT/OBJECT is Null or not, 
	 * if it NULL pass the STEP, Otherwise fail the STEP
	 * 
	 * 2. static assertNotNull(Object, Object)
	 * 
	 * 'static assertNotNull' method verifies whether the given INPUT/OBJECT is NotNull or not, 
	 * if it NotNULL pass the STEP, Otherwise fail the STEP
	 * 
	 * 3. static assertEquals(String, String)
	 * 
	 * 'static assertEquals' method verifies whether the given BOTH the VALUES are EQUAL , 
	 * then pass the STEP, Otherwise fail the STEP
	 * 
	 * 4. static assertNotEquals(String, String)
	 * 
	 * 'static assertNotEquals' method verifies whether the given BOTH the VALUES are NotEQUAL , 
	 * then pass the STEP, Otherwise fail the STEP
	 * 
	 * 5. static assertTrue(boolean condition)
	 * 
	 * "assertTrue" method If the GIVEN CONDTION is TRUE then PASS the STEP, 
	 *  Otherwise fail the STEP
	 * 
	 * 6. static assertFalse(boolean condition)
	 * 
	 * "static assertFalse" method If the GIVEN CONDTION is FALSE then PASS the STEP, 
	 *  Otherwise fail the STEP
	 *  
	 * 
	 * B. SoftAssert
	 * 
	 * "SoftAssert" is also called 'Instance MEthod'
	 * Create an Object/instance.
	 * 
	 * EVEN "ASSERTION FAILED" CONTUNUE with the REST of the PROGRAM
	 * 
	 * 
	 * 
	 * 1. assertNull(Object, Object)
	 * 
	 * 2. assertNotNull(Object, Object)
	 * 
	 * 3. assertEquals(String, String)
	 * 
	 * 4. assertNotEquals(String, String)
	 * 
	 * 5. assertTrue(boolean condition)
	 * 
	 * 6. assertFalse(boolean condition)
	 * 
	 * 
	 */
	

}
